/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['www.gstatic.com', 'sunilpratapsingh.com', 'www.yourdigitalweb.com', 'i.pinimg.com', 'indeedseo.com', 'en.myposeo.com', 'cdn.dribbble.com', 'i.gifer.com'], // Add the hostname here for external images
  },
};

export default nextConfig;
